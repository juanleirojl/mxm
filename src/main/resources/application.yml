server:
  port: ${SERVER_PORT:8080}
  max-http-request-header-size: 65536
  tomcat:
    max-swallow-size: 2MB
      
    

spring:
  application:
    name: mxm-cimentos
  datasource:
    url: jdbc:postgresql://145.223.94.252:5432/mxm
    username: mxm
    password: mxm
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  #mvc:
    #servlet:
      #path: /api
  web:
   locale: pt_BR
   locale-resolver: fixed
   resources:
     static-locations: classpath:/static/ 
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    cache: false